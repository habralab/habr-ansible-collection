{% if nginx_log_format is defined and nginx_log_format | length > 0 %}
### Log formats
{% for log in nginx_log_format if log.name != "main" %}

log_format	{{ log.name }}{% if log.escape is defined %} escape={{ log.escape }}{% endif %}{{ '' }}
{% for string in log.string %}
		'{{ string }}'{% if loop.last %};{% endif %}{{ '' }}
{% endfor %}
{% endfor %}
{% endif %}
