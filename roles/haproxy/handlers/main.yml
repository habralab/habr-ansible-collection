---
- name: "Restart haproxy"
  ansible.builtin.systemd:
    name: haproxy
    state: restarted
  ignore_errors: "{{ ansible_check_mode }}"

- name: "Reload haproxy"
  ansible.builtin.systemd:
    name: haproxy
    state: reloaded
  ignore_errors: "{{ ansible_check_mode }}"
